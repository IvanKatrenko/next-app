
// component provider to komponent ktory umozliwia uzycie danego contextu wewnatrz
// component provider is a component that enables the use of a given context inside


import context from "react";

export Provider = () => {
    
}